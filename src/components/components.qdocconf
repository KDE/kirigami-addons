include($KDE_DOCS/global/qt-module-defaults.qdocconf)

project = KirigamiAddonsComponents
description = Todo

documentationinheaders = true

headerdirs += .
sourcedirs += .
imagedirs  += ../../docs/pics

outputformats = HTML

depends += kde kirigami kirigamiplatform qtcore qtquick qtquickcontrols qtgui qtmultimedia
